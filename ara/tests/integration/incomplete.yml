- name: Incomplete playbook
  hosts: localhost
  tasks:
    - debug:
        msg: "This playbook is meant to be interrupted"
    - name: Sleep for thirty seconds
      command: sleep 30
      changed_when: false
