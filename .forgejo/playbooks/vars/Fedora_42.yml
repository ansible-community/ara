ci_user: fedora
ci_group: fedora

# Dependencies expected to be installed
ci_requirements:
  - git
  - python3
  - python3-pip
  - python3-wheel

# Name of the image for the VM in OpenStack
os_image: fedora-42
